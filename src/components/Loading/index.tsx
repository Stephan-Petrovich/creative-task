import { ReactElement } from "react";
import "./style.css";

const Loading = (): ReactElement => {
    return <div className="loading-alert-box">Wait a second, loading...</div>;
};

export default Loading;
